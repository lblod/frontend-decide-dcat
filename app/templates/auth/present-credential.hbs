{{page-title "Access the data"}}

<Auth::WalletInteraction
  @title="Access the data"
  @backRoute="auth.index"
  @pendingMessage={{this.pendingMessage}}
  @uriFetchUrl={{this.uriFetchUrl}}
  @statusPollUrl={{this.statusPollUrl}}
  @prohibitAuth={{this.prohibitAuth}}
/>